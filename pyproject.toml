[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lumen-sdk"
version = "0.4.0"
description = "Telemetry integrity and forensic audit SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiosqlite"
]
